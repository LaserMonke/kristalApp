name: optionsschool
description: "Stock Options Academy — an educational app teaching how options and derivatives work."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# The number after "+" is the Android versionCode and the iOS build number.
# Play REJECTS an upload whose versionCode already exists on the track, so this
# has to go up for every bundle you upload — even a rebuild of identical code.
# The part before "+" is what a learner sees; it only changes when you decide
# the release is worth a new name.
version: 1.0.1+6

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.1
  go_router: ^17.3.0
  fl_chart: ^1.2.0
  shared_preferences: ^2.5.5
  flutter_local_notifications: ^22.2.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  supabase_flutter: ^2.16.0
  flutter_dotenv: ^6.0.1
  http: ^1.6.0
  audioplayers: ^6.8.1
  printing: ^5.15.0
  pdf: ^3.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Already pulled in transitively by flutter_riverpod; declared directly so
  # tests can import package:riverpod/misc.dart for the ProviderScope
  # `Override` type when building parameterised provider overrides.
  riverpod: ^3.4.1

  # Generates the platform launcher icons from assets/branding/. Build-time
  # only — regenerate with `dart run flutter_launcher_icons` after editing the
  # source PNGs, and commit the generated mipmaps.
  flutter_launcher_icons: ^0.14.4

# Source images live in assets/branding/ and are deliberately NOT registered as
# Flutter assets — they are inputs to icon generation, not things the app loads
# at runtime, and bundling them would just pad the download.
flutter_launcher_icons:
  android: true
  ios: true # Generated here so the iOS mark matches Android's, not the Flutter default.
  image_path: assets/branding/app_icon.png
  # iOS icons may not carry an alpha channel (App Store rejects it). Flatten any
  # transparency onto the brand dark so the mark still reads on it.
  remove_alpha_ios: true
  background_color_ios: "#0B0E13"
  # Adaptive icons: Android masks the foreground to a circle/squircle and crops
  # the outer third, so the payoff mark is inset in its own foreground file.
  # The background is the flat terminal-dark from AppColors.darkBackground.
  adaptive_icon_background: "#0B0E13"
  adaptive_icon_foreground: assets/branding/app_icon_foreground.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Lesson content is data, not code (see CLAUDE.md): adding a lesson means
  # editing the JSON below, not the engine.
  assets:
    - assets/lessons/
    # The intro chime, synthesised for this app (see DEPLOY.md) — not sampled
    # from anywhere.
    - assets/audio/
    # Embedded in the printed certificate so a learner's name renders whatever
    # alphabet it is written in — the PDF built-in fonts are ASCII only.
    # Roboto, Apache-2.0; licence sits beside it.
    - assets/fonts/
    # Stockle's playable ticker set. Data, not code — and it carries its own
    # as_of date because index membership drifts (CLAUDE.md rule 8).
    - assets/games/
    # Supabase URL + publishable key (flutter_dotenv reads it as an asset).
    # Gitignored; see .env.example. Safe to bundle ONLY because RLS is on —
    # the service_role key never goes in here.
    - .env

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
